#include <stdio.h> // incluye información acerca de la biblioteca estándar

int main(void) { // define una función llamada main, nuestra función especial dende comienza la ejecución del programa, que no recibe valores de argumentos. Las proposicionse de main están encerradas entre llaves.
	printf("Hola, mundo!!!\n"); // main llama a la función de biblioteca printf() para escribir esta secucuencia de caracteres; \n representa el carácter nueva línea
	return 0; // return 0; indica que la función main terminó con éxito
}

